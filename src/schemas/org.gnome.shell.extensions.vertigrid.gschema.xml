<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.vertigrid" path="/org/gnome/shell/extensions/vertigrid/">

    <key name="animate-scroll" type="b">
      <default>true</default>
    </key>

    <key name="app-sorting" type="s">
      <default>'usage'</default>
    </key>

    <key name="columns" type="i">
      <default>6</default>
    </key>

    <key name="favorites-section" type="b">
      <default>true</default>
    </key>

    <key name="favorites-sorting" type="s">
      <default>'dash'</default>
    </key>

    <key name="category-grouping" type="b">
      <default>true</default>
    </key>

    <key name="show-favorites-in-app-grid" type="b">
      <default>false</default>
    </key>

    <key name="icon-size" type="i">
      <default>64</default>
    </key>

    <key name="icon-spacing" type="i">
      <default>24</default>
    </key>

    <key name="category-font-size" type="i">
      <default>16</default>
    </key>

    <key name="show-workspaces" type="b">
      <default>true</default>
    </key>

    <key name="app-category-overrides" type="as">
      <default>[]</default>
      <summary>Per-app category overrides in the form 'appId::Category'</summary>
    </key>

    <key name="custom-categories" type="s">
      <default>'[]'</default>
      <summary>Custom categories as a JSON array of objects</summary>
    </key>

    <key name="always-show-category-nav" type="b">
      <default>false</default>
      <summary>Always show category navigation</summary>
      <description>Keep the left category navigation expanded at all times instead of only on hover.</description>
    </key>

    <key name="clip-app-labels" type="b">
      <default>false</default>
      <summary>Clip long app names</summary>
      <description>Clip long application names to a single line instead of showing them in full.</description>
    </key>

    <key name="hidden-apps" type="as">
      <default>[]</default>
      <summary>Apps hidden from this extension's app grid</summary>
      <description>List of application IDs (.desktop file names) hidden from this extension's app grid via its right-click menu. Does not affect other launchers, search, or the system app menu.</description>
    </key>

    <key name="blur-app-grid-background" type="b">
      <default>true</default>
      <summary>Blur the wallpaper behind the app grid</summary>
      <description>Show a blurred wallpaper backdrop behind the app grid, matching the workspaces view's own background.</description>
    </key>
   
  </schema>
</schemalist>
